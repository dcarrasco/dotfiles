# INSTALL NEW DEBIAN
 Usar imagen netinstall de DEBIAN
 Instalar con btrfs separando / y /home
pdf con pasos a seguir

 Bootear
 Instalar sudo y configurar sudo
 su root
 apt install sudo
 /sbin/usermod -aG sudo daniel

Install git
recuperar dotfiles: git clone https://github.com/dcarrasco/dotfiles

