#   _   _                  _                 _
#  | | | |_   _ _ __  _ __| | __ _ _ __   __| |
#  | |_| | | | | '_ \| '__| |/ _` | '_ \ / _` |
#  |  _  | |_| | |_) | |  | | (_| | | | | (_| |
#  |_| |_|\__, | .__/|_|  |_|\__,_|_| |_|\__,_|
#         |___/|_|

# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
monitor=,preferred,auto,1

#
# Some default env vars.
env = XCURSOR_SIZE,24
env = GTK_THEME,Yaru-purple
env = QT_QPA_PLATFORM, wayland


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = GTK_THEME=Nordic waybar
exec-once = lxpolkit
# exec-once = dunst
exec-once = swaybg & /usr/bin/variety
exec-once = dropbox start
exec-once = hyprctl setcursor 'macOS-BigSur' 32
exec-once = xhost +SI:localuser:root
exec-once = swayidle -w timeout 180 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'
# exec-once = flameshot
#

# Source a file (multi-file configs)
source = ~/.config/hypr/keybinds.conf
source = ~/.config/hypr/colors.conf

#
# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = latam,us
    kb_variant =
    kb_model =
    # kb_options = grp:alt_caps_toggle, ctrl:swapcaps    #
    kb_options = grp:alt_caps_toggle
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 7
    gaps_out = 15
    border_size = 4
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.active_border = rgb($nord8) rgb($nord10) 45deg
    # col.active_border = rgb($nord15)
    col.inactive_border = rgb($nord2)

    # layout = dwindle
    layout = master

    # resize_on_border = true
    # hover_icon_on_border = true
}

group {
    col.border_active = rgb($nord12)
    col.border_inactive = rgb($nord1)
    groupbar {
        enabled = true
        render_titles = false
        col.active = rgb($nord12)
        col.inactive = rgb($nord1)
    }
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 15

    blur {
        enabled = true
        size = 6
        passes = 2
        new_optimizations = on
        # xray = true
    }

    drop_shadow = yes
    shadow_range = 25
    shadow_render_power = 2
    # shadow_ignore_window = 1
    col.shadow = rgba($nord0BB)
    col.shadow_inactive = rgba(00000000)
    shadow_offset = 3 3
    # dim_inactive = true
    # dim_strength = 0.1
    # inactive_opacity = 0.8
}


animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = smoothOut, 0.5, 0, 0.99, 0.99
    bezier = slow, 0, 0.85, 0.3, 1

    animation = windowsIn, 1, 5, myBezier
    animation = windowsOut, 1, 5, myBezier, popin 80%
    # animation = windowsOut, 1, 3, smoothOut
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 5, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = false # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
    force_split = 2
    use_active_for_splits = true
    no_gaps_when_only = 0   # 0 with gaps / 1 no gaps no border / 2 no gaps with border
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    mfact = 0.55
    new_is_master = true
    no_gaps_when_only = 1   # 0 with gaps / 1 no gaps no border / 2 no gaps with border
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

device {
    name = msft0001:00-04f3:317c-touchpad
    enabled = true
    natural_scroll = yes
}

# Example windowrule v1
windowrule = float, ^(org.gnome.Calculator)$
windowrule = float, ^(variety)$
# Example windowrule v2
windowrulev2 = float, class:^(dosbox)$
windowrulev2 = workspace 1,class:^(brave-browser)$
windowrulev2 = workspace 1,class:^(firefox)$
windowrulev2 = workspace 3,class:^(virt-manager)$
windowrulev2 = workspace 3,class:^(virt-viewer)$
windowrulev2 = fakefullscreen, class:^(virt-viewer)$
windowrulev2 = workspace 4,class:^(thunar)$
windowrulev2 = pin,class:^(Rofi)$
windowrulev2 = pin,class:^(wofi)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

layerrule = blur, waybar
layerrule = blur, rofi

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    # suppress_portal_warnings = true
}

